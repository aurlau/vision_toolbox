vision_toolbox

This repository is used in order to contain various functionn and class for image processing and analysis

Next step

Use src layout: src/vision_toolbox, tests/, pyproject.toml (PEP 621 metadata, dependencies), and init.py.
Add one simple, well-typed function (e.g., image resize or normalization) and a unit test.
Add pre-commit with ruff, black, isort, mypy; run hooks locally and fix issues.
Done when: pre-commit passes locally, pytest passes locally, CI is green on the repo.****
